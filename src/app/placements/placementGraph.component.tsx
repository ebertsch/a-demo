import { Placement } from "./placement";

export interface PlacementGraphProps {
    placements: Placement[];
}

export const PlacementGraph = (props: PlacementGraphProps) => {
    return (
    <div>
        Graph here
    </div>
    );
} 